# 🚀 Запуск серверов

## ⚠️ Важно: Backend должен быть запущен!

Для работы авторизации через Yandex и всех функций сайта необходимо запустить backend сервер.

## 📋 Быстрый запуск:

### Вариант 1: Использовать batch файлы

1. **Запустите backend:**
   ```bash
   start-backend.bat
   ```
   Или двойной клик по файлу `start-backend.bat`

2. **В другом терминале запустите frontend:**
   ```bash
   start-frontend.bat
   ```
   Или двойной клик по файлу `start-frontend.bat`

### Вариант 2: Ручной запуск

1. **Backend (Laravel):**
   ```bash
   php artisan serve
   ```
   Сервер запустится на: http://localhost:8000

2. **Frontend (Vite):**
   ```bash
   cd frontend
   npm run dev
   ```
   Сервер запустится на: http://localhost:5173

## ✅ Проверка статуса:

Запустите:
```bash
check-backend.bat
```

Или проверьте вручную:
```bash
curl http://localhost:8000/health
```

Должен вернуться: `{"status":"ok","timestamp":"..."}`

## 🔧 Решение проблем:

### Ошибка: "Не удалось подключиться к серверу"

1. **Проверьте, запущен ли backend:**
   - Откройте http://localhost:8000/health в браузере
   - Должен вернуться JSON ответ

2. **Если backend не запущен:**
   ```bash
   php artisan serve
   ```

3. **Если порт 8000 занят:**
   ```bash
   php artisan serve --port=8001
   ```
   И обновите `VITE_API_URL` в `frontend/.env`

4. **Проверьте CORS:**
   - Убедитесь, что в `config/cors.php` указан `http://localhost:5173`

### Ошибка: "CORS policy"

1. Очистите кеш Laravel:
   ```bash
   php artisan config:clear
   php artisan cache:clear
   ```

2. Перезапустите backend сервер

## 📝 Порты по умолчанию:

- **Backend:** http://localhost:8000
- **Frontend:** http://localhost:5173

## 🎯 После запуска:

1. Откройте http://localhost:5173
2. Вы увидите страницу авторизации/регистрации
3. Нажмите "Войти через Yandex" для авторизации

