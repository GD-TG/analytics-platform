# Полная инструкция по настройке проекта

## ✅ Все задачи выполнены!

### Что реализовано:

1. ✅ **Сайдбар** - скрыт по умолчанию, раскрывается при наведении, с подменю
2. ✅ **Реальные данные** - подключение к API вместо тестовых данных
3. ✅ **Цветовая индикация месяцев** - 🟢🟡🔴 на основе динамики метрик
4. ✅ **Hover-сравнение** - показ изменений при наведении на метрики
5. ✅ **Аналитический термометр** - индикатор статуса проектов (🔥🌤❄)
6. ✅ **Адаптивный дизайн** - поддержка мобильных устройств
7. ✅ **База данных MySQL** - миграции созданы
8. ✅ **Авторизация/Регистрация** - полная система с Laravel Sanctum
9. ✅ **AI анализ** - интеграция с Hugging Face для анализа данных

## 🚀 Быстрый старт

### 1. Настройка базы данных

```bash
# Создайте базу данных MySQL
mysql -u root -p
CREATE DATABASE analytics_platform CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
exit;

# Настройте .env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=analytics_platform
DB_USERNAME=your_username
DB_PASSWORD=your_password

# Выполните миграции
php artisan migrate
```

### 2. Настройка API ключей

Добавьте в `.env`:
```env
# Yandex API
YANDEX_OAUTH_TOKEN=your_token_here
DIRECT_CLIENT_LOGIN=your_client_login

# Hugging Face AI (опционально)
HUGGINGFACE_API_KEY=your_api_key_here
```

### 3. Установка зависимостей

```bash
# Backend
composer install

# Frontend
cd frontend
npm install
```

### 4. Запуск проекта

```bash
# Backend (в одном терминале)
php artisan serve

# Frontend (в другом терминале)
cd frontend
npm run dev
```

### 5. Создание первого пользователя

```bash
php artisan tinker
```

```php
$user = \App\Models\User::create([
    'name' => 'Admin',
    'email' => 'admin@example.com',
    'password' => bcrypt('password'),
    'role' => 'admin',
]);
```

## 📁 Структура проекта

### Backend (Laravel)
- `app/Http/Controllers/` - контроллеры
- `app/Models/` - модели Eloquent
- `app/Services/` - бизнес-логика
- `app/Helpers/` - вспомогательные классы
- `database/migrations/` - миграции БД
- `routes/api.php` - API маршруты

### Frontend (React)
- `frontend/src/pages/` - страницы приложения
- `frontend/src/components/` - компоненты
- `frontend/src/api/` - API клиент
- `frontend/src/pages/Login/` - страница входа
- `frontend/src/pages/Register/` - страница регистрации

## 🔐 API Endpoints

### Публичные
- `POST /auth/register` - регистрация
- `POST /auth/login` - вход
- `GET /health` - проверка здоровья

### Защищенные (требуют токен)
- `POST /auth/logout` - выход
- `GET /auth/me` - текущий пользователь
- `GET /api/report/{id}` - отчет проекта
- `GET /api/statistics` - статистика
- `GET /api/visits` - данные визитов
- `GET /api/sources` - источники трафика
- `GET /api/age-data` - возрастные данные
- `GET /api/projects-thermometer` - термометр проектов
- `GET /api/ai/analyze/{project}` - AI анализ

## 🎨 Основные функции

### Цветовая индикация месяцев
- 🟢 Зеленый - рост ключевых метрик
- 🟡 Желтый - стабильность
- 🔴 Красный - падение метрик

### Аналитический термометр
- 🔥 Проект растет
- 🌤 Стабильно
- ❄ Есть падения

### Hover-сравнение
При наведении на метрику показывается:
- Было: X → Стало: Y
- Разница: +Z / +N%

## 📱 Адаптивность

Все страницы адаптированы для мобильных устройств:
- Сайдбар скрывается на мобильных
- Таблицы с горизонтальной прокруткой
- Адаптивные сетки карточек

## 🤖 AI Анализ

Используется модель **T5-small** от Hugging Face:
- Размер: ~240MB
- Требования: ~1-2GB RAM
- Использование через API или локально

Подробнее: см. `SETUP_AI.md`

## 📝 Дополнительные инструкции

- `SETUP_DATABASE.md` - настройка БД
- `SETUP_API.md` - настройка API интеграций
- `SETUP_AI.md` - настройка AI модели

## 🐛 Решение проблем

### Ошибка подключения к БД
Проверьте настройки в `.env` и убедитесь, что MySQL запущен.

### Ошибка авторизации
Убедитесь, что миграции выполнены и таблица `users` существует.

### API не работает
Проверьте, что токены Yandex API настроены в `.env`.

### AI анализ не работает
Проверьте `HUGGINGFACE_API_KEY` в `.env` или используйте fallback режим.

## 📞 Поддержка

При возникновении проблем проверьте логи:
- Backend: `storage/logs/laravel.log`
- Frontend: консоль браузера (F12)

