# Инструкция по исправлению ошибки "Failed to fetch"

## ✅ Что уже исправлено:

1. ✅ Создан файл `config/cors.php` с правильными настройками
2. ✅ Улучшена обработка ошибок в HTTP клиенте
3. ✅ Улучшена обработка ошибок в контроллерах

## 🔧 Что нужно сделать вручную:

### Шаг 1: Очистите кеш Laravel

Запустите файл `setup.bat` или выполните вручную:
```bash
php artisan config:clear
php artisan cache:clear
php artisan route:clear
php artisan optimize:clear
```

### Шаг 2: Убедитесь, что база данных настроена

Запустите файл `migrate.bat` или выполните:
```bash
php artisan migrate
```

### Шаг 3: Перезапустите серверы

1. **Остановите** текущие серверы (если запущены)
2. **Запустите заново:**
   - `start-backend.bat` - для backend
   - `start-frontend.bat` - для frontend
   - Или `start.bat` - для обоих

### Шаг 4: Проверьте настройки

Запустите `check-setup.bat` для проверки всех настроек.

## 🔍 Проверка работы:

1. **Backend должен отвечать:**
   - Откройте: http://localhost:8000/health
   - Должен вернуть: `{"status":"ok","timestamp":"..."}`

2. **Frontend должен открываться:**
   - Откройте: http://localhost:5173
   - Должна открыться страница входа

3. **Проверьте консоль браузера (F12):**
   - Не должно быть ошибок CORS
   - Запросы должны проходить успешно

## ⚠️ Если ошибка все еще есть:

1. **Проверьте, что оба сервера запущены:**
   - Backend на порту 8000
   - Frontend на порту 5173

2. **Проверьте файл `config/cors.php`:**
   - Должен содержать `http://localhost:5173` в `allowed_origins`

3. **Проверьте `.env` файл:**
   - `APP_URL` должен быть `http://localhost:8000` (или не указан)

4. **Проверьте консоль браузера:**
   - Откройте F12 → Console
   - Посмотрите точное сообщение об ошибке

## 📝 Типичные проблемы:

- **"Network error"** → Backend не запущен или недоступен
- **"CORS error"** → Проблема с настройками CORS (проверьте `config/cors.php`)
- **"401 Unauthorized"** → Проблема с токеном (нормально для первого входа)
- **"422 Validation error"** → Ошибка валидации данных (проверьте введенные данные)

