# 🔧 Решение проблемы "Не удалось подключиться к серверу"

## ❌ Проблема:
Ошибка: "Не удалось подключиться к серверу. Убедитесь, что backend запущен на http://localhost:8000"

## ✅ Решение:

### Шаг 1: Проверьте, запущен ли backend

**Вариант А: Через командную строку**
```bash
netstat -ano | findstr :8000
```
Если порт занят - сервер запущен. Если нет - нужно запустить.

**Вариант Б: Через браузер**
Откройте: http://localhost:8000/health
- Если видите JSON с `{"status":"ok"}` - сервер работает ✅
- Если ошибка подключения - сервер не запущен ❌

### Шаг 2: Запустите backend сервер

**Способ 1: Через batch файл (рекомендуется)**
```bash
start-backend.bat
```

**Способ 2: Вручную**
```bash
php artisan serve
```

**Способ 3: С указанием хоста и порта**
```bash
php artisan serve --host=127.0.0.1 --port=8000
```

### Шаг 3: Проверьте работу

1. Откройте новый терминал/окно
2. Выполните: `curl http://localhost:8000/health`
3. Или откройте в браузере: http://localhost:8000/health

Должен вернуться JSON: `{"status":"ok","timestamp":"..."}`

### Шаг 4: Если порт занят другим приложением

Если порт 8000 занят, используйте другой порт:

```bash
php artisan serve --port=8001
```

Затем обновите URL в frontend:
- Создайте файл `frontend/.env`
- Добавьте: `VITE_API_URL=http://localhost:8001`

## 🔍 Диагностика проблем:

### Проблема 1: PHP не найден
**Решение:**
- Убедитесь, что PHP установлен
- Проверьте: `php -v`
- Добавьте PHP в PATH системы

### Проблема 2: Laravel не настроен
**Решение:**
```bash
composer install
php artisan key:generate
```

### Проблема 3: Порт занят
**Решение:**
- Найдите процесс: `netstat -ano | findstr :8000`
- Остановите процесс или используйте другой порт

### Проблема 4: Ошибки в Laravel
**Решение:**
```bash
php artisan config:clear
php artisan cache:clear
php artisan route:clear
```

## 📝 Быстрая проверка:

Запустите `test-backend.bat` для автоматической проверки.

## ✅ После запуска backend:

1. Backend должен быть доступен на http://localhost:8000
2. Frontend должен быть на http://localhost:5173
3. Оба должны работать одновременно

## 🎯 Итоговая проверка:

1. ✅ Backend запущен: http://localhost:8000/health
2. ✅ Frontend запущен: http://localhost:5173
3. ✅ Оба сервера работают одновременно
4. ✅ Попробуйте зарегистрироваться/войти

