# ✅ Финальный чеклист настройки

## 🎯 Все исправления применены:

1. ✅ **CORS настроен** - файл `config/cors.php` создан
2. ✅ **Обработка ошибок улучшена** - в HTTP клиенте и контроллерах
3. ✅ **Sanctum добавлен** - в `composer.json`
4. ✅ **Batch файлы созданы** - для автоматизации

## 📋 Что нужно сделать СЕЙЧАС:

### 1. Установить Sanctum (если еще не установлен)

```bash
composer require laravel/sanctum
```

Или запустите:
```bash
composer install
```

### 2. Очистить кеш Laravel

Запустите `setup.bat` или выполните:
```bash
php artisan config:clear
php artisan cache:clear
php artisan route:clear
```

### 3. Выполнить миграции

Запустите `migrate.bat` или выполните:
```bash
php artisan migrate
```

### 4. Перезапустить серверы

**Остановите** текущие серверы (если запущены) и запустите заново:
- `start.bat` - для обоих серверов
- Или отдельно: `start-backend.bat` и `start-frontend.bat`

## 🔍 Проверка работы:

1. **Backend:** http://localhost:8000/health
   - Должен вернуть JSON с `status: "ok"`

2. **Frontend:** http://localhost:5173
   - Должна открыться страница входа

3. **Попробуйте зарегистрироваться:**
   - Если ошибка "Failed to fetch" - проверьте консоль браузера (F12)
   - Убедитесь, что backend запущен на порту 8000

## 🐛 Если ошибка "Failed to fetch" все еще есть:

### Шаг 1: Проверьте консоль браузера
- Откройте F12 → Console
- Посмотрите точное сообщение об ошибке
- Проверьте вкладку Network - какой запрос не проходит

### Шаг 2: Проверьте серверы
- Backend должен быть на http://localhost:8000
- Frontend должен быть на http://localhost:5173
- Оба должны быть запущены

### Шаг 3: Проверьте CORS
- Файл `config/cors.php` должен содержать `http://localhost:5173`
- После изменения запустите `setup.bat` для очистки кеша

### Шаг 4: Проверьте базу данных
- Убедитесь, что миграции выполнены
- Проверьте подключение к БД в `.env`

## 📝 Созданные файлы для помощи:

- `setup.bat` - очистка кеша
- `migrate.bat` - миграции БД
- `check-setup.bat` - проверка окружения
- `install-dependencies.bat` - установка зависимостей
- `start.bat` - запуск серверов
- `start-backend.bat` - запуск backend
- `start-frontend.bat` - запуск frontend

## 🎉 После успешной настройки:

1. Создайте первого пользователя (см. `README_SETUP.md`)
2. Войдите в систему
3. Наслаждайтесь работой приложения!

