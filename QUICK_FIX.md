# ⚡ Быстрое исправление ошибки "Failed to fetch"

## ✅ Что уже сделано:

1. ✅ Создан `config/cors.php` с правильными настройками
2. ✅ Улучшена обработка ошибок
3. ✅ Добавлен Sanctum в зависимости

## 🚀 Что нужно сделать ПРЯМО СЕЙЧАС:

### Вариант 1: Автоматически (рекомендуется)

Запустите эти файлы **по порядку**:

1. **`install-dependencies.bat`** - установит Sanctum и другие зависимости
2. **`setup.bat`** - очистит кеш (ВАЖНО!)
3. **`migrate.bat`** - создаст таблицы в БД
4. **`start.bat`** - запустит оба сервера

### Вариант 2: Вручную

```bash
# 1. Установить Sanctum
composer require laravel/sanctum

# 2. Очистить кеш
php artisan config:clear
php artisan cache:clear
php artisan route:clear

# 3. Выполнить миграции
php artisan migrate

# 4. Запустить серверы
# В одном терминале:
php artisan serve

# В другом терминале:
cd frontend
npm run dev
```

## 🔍 Проверка:

1. Откройте http://localhost:8000/health - должен вернуть JSON
2. Откройте http://localhost:5173 - должна открыться страница входа
3. Попробуйте зарегистрироваться

## ⚠️ Если все еще ошибка:

1. **Откройте консоль браузера (F12)**
2. **Посмотрите точное сообщение об ошибке**
3. **Проверьте вкладку Network** - какой запрос не проходит
4. **Убедитесь, что backend запущен** на порту 8000

## 📝 Важно:

После изменения `config/cors.php` **ОБЯЗАТЕЛЬНО** запустите `setup.bat` для очистки кеша!

