# 🚀 Быстрый старт

## ⚠️ ВАЖНО: Запустите Backend перед использованием!

## 📋 Шаг 1: Запуск Backend

**Откройте терминал в корне проекта и выполните:**

```bash
php artisan serve
```

Или используйте batch файл:
```bash
start-backend.bat
```

**Backend должен запуститься на:** http://localhost:8000

## 📋 Шаг 2: Запуск Frontend

**Откройте новый терминал и выполните:**

```bash
cd frontend
npm run dev
```

Или используйте batch файл:
```bash
start-frontend.bat
```

**Frontend должен запуститься на:** http://localhost:5173

## ✅ Проверка

1. Откройте http://localhost:5173 в браузере
2. Вы увидите страницу авторизации/регистрации
3. Нажмите "Войти через Yandex"

## 🔧 Если видите ошибку "Не удалось подключиться к серверу"

1. **Проверьте, запущен ли backend:**
   - Откройте http://localhost:8000/health в браузере
   - Должен вернуться: `{"status":"ok","timestamp":"..."}`

2. **Если backend не запущен:**
   ```bash
   php artisan serve
   ```

3. **Если порт 8000 занят:**
   ```bash
   php artisan serve --port=8001
   ```

## 📝 Автоматический запуск обоих серверов

Используйте:
```bash
start.bat
```

Это запустит оба сервера в отдельных окнах.
