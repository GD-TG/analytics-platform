# ⚠️ Backend сервер не запущен!

## 🔴 Проблема:

Не удается открыть http://localhost:8000/health

Это означает, что **backend сервер не запущен**.

## ✅ Решение:

### Шаг 1: Запустите Backend

**Вариант А: Двойной клик по файлу**
- Найдите файл `RUN_BACKEND_NOW.bat` в корне проекта
- Двойной клик по нему

**Вариант Б: В терминале**
```bash
php artisan serve
```

**Вариант В: Используйте batch файл**
```bash
start-backend.bat
```

### Шаг 2: Проверьте работу

После запуска должно появиться:
```
INFO  Server running on [http://127.0.0.1:8000]
```

### Шаг 3: Проверьте в браузере

Откройте: http://localhost:8000/health

Должен вернуться:
```json
{"status":"ok","timestamp":"..."}
```

## 🔧 Если не запускается:

### Ошибка: "php не является командой"

**Решение:**
1. Установите PHP: https://www.php.net/downloads
2. Или используйте XAMPP (PHP уже включен)
3. Добавьте PHP в PATH системы

### Ошибка: "Class not found"

**Решение:**
```bash
composer install
```

### Ошибка: "Порт занят"

**Решение:**
```bash
php artisan serve --port=8001
```

## 📝 После запуска:

1. **Оставьте окно backend открытым** (не закрывайте!)
2. **Запустите frontend** в другом терминале:
   ```bash
   cd frontend
   npm run dev
   ```
3. **Откройте:** http://localhost:5173

## 🎯 Автоматический запуск обоих:

Используйте:
```bash
start.bat
```

Это запустит backend и frontend одновременно.

