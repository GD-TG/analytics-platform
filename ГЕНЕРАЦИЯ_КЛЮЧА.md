# 🔑 Генерация APP_KEY

## В CMD выполните:

```cmd
cd C:\PlanicaTask\analytics-platform
php generate-app-key.php
```

Или напрямую:

```cmd
php artisan key:generate
```

## Что произойдет:

1. Создастся или обновится файл `.env`
2. Будет сгенерирован уникальный `APP_KEY`
3. Ключ будет добавлен в `.env` файл

## После генерации:

Очистите кеш:

```cmd
php artisan config:clear
php artisan cache:clear
```

## Если команды не работают:

Убедитесь, что:
- PHP установлен и доступен в PATH
- Вы находитесь в папке `C:\PlanicaTask\analytics-platform`
- Файл `artisan` существует в этой папке

## Проверка:

После генерации ключа перезапустите backend и проверьте:
- http://localhost:8000/health

