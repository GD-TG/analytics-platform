import BasePage from '../BasePage/BasePage';

const Documents = () => (
  <BasePage
    title="Документы"
    subtitle="Управление документами"
    renderContent={() => (
      <div>
        <p>Документы будут отображаться здесь после настройки API</p>
      </div>
    )}
  />
);

export default Documents;

