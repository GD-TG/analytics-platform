import BasePage from '../BasePage/BasePage';

const Resources = () => (
  <BasePage
    title="Ресурсы"
    subtitle="Управление ресурсами"
    renderContent={() => (
      <div>
        <p>Данные о ресурсах будут отображаться здесь после настройки API</p>
      </div>
    )}
  />
);

export default Resources;

