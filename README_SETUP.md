# 🎯 Быстрая настройка проекта

## ⚡ Автоматическая настройка (рекомендуется)

Запустите файлы по порядку:

1. **`check-setup.bat`** - проверка окружения
2. **`install-dependencies.bat`** - установка зависимостей (если нужно)
3. **`migrate.bat`** - настройка базы данных
4. **`setup.bat`** - очистка кеша
5. **`start.bat`** - запуск серверов

## 📝 Ручная настройка

### 1. Установка зависимостей

**Backend:**
```bash
composer install
```

**Frontend:**
```bash
cd frontend
npm install
```

### 2. Настройка базы данных

1. Создайте БД MySQL: `analytics_platform`
2. Настройте `.env` файл с параметрами БД
3. Выполните миграции: `php artisan migrate`

### 3. Очистка кеша

```bash
php artisan config:clear
php artisan cache:clear
php artisan route:clear
php artisan optimize:clear
```

### 4. Запуск серверов

**Backend:**
```bash
php artisan serve
```

**Frontend:**
```bash
cd frontend
npm run dev
```

## 🔧 Исправление ошибки "Failed to fetch"

Все исправления уже применены:
- ✅ CORS настроен в `config/cors.php`
- ✅ Обработка ошибок улучшена
- ✅ Контроллеры возвращают правильные ответы

**Если ошибка все еще есть:**

1. Запустите `setup.bat` для очистки кеша
2. Убедитесь, что оба сервера запущены
3. Проверьте консоль браузера (F12) для деталей ошибки

## 📍 Доступ к приложению

- **Frontend:** http://localhost:5173
- **Backend API:** http://localhost:8000
- **Health Check:** http://localhost:8000/health

## 🔐 Первый вход

Создайте пользователя:
```bash
php artisan tinker
```
```php
\App\Models\User::create([
    'name' => 'Admin',
    'email' => 'admin@example.com',
    'password' => bcrypt('password'),
    'role' => 'admin',
]);
```

Затем войдите на http://localhost:5173/login

## 📚 Дополнительная документация

- `COMPLETE_SETUP.md` - полная инструкция
- `FIX_CORS.md` - решение проблем с CORS
- `SETUP_DATABASE.md` - настройка БД
- `SETUP_AI.md` - настройка AI

