# 🚀 Как запустить Backend сервер

## ⚠️ ВАЖНО: Backend должен быть запущен для работы сайта!

## 📋 Способ 1: Использовать batch файл (САМЫЙ ПРОСТОЙ)

**Двойной клик по файлу:** `start-backend.bat`

Или в терминале:
```bash
start-backend.bat
```

## 📋 Способ 2: Запуск вручную

1. **Откройте терминал (PowerShell или CMD) в папке проекта:**
   ```
   C:\PlanicaTask\analytics-platform
   ```

2. **Выполните команду:**
   ```bash
   php artisan serve
   ```

3. **Должно появиться сообщение:**
   ```
   INFO  Server running on [http://127.0.0.1:8000]
   ```

## ✅ Проверка работы:

После запуска откройте в браузере:
- http://localhost:8000/health

Должен вернуться ответ:
```json
{"status":"ok","timestamp":"2024-..."}
```

## 🔧 Если не работает:

### Проблема: "php не является внутренней или внешней командой"

**Решение:** PHP не установлен или не добавлен в PATH.

1. Установите PHP: https://www.php.net/downloads
2. Или используйте XAMPP/WAMP (PHP уже включен)

### Проблема: "Порт 8000 уже занят"

**Решение:** Запустите на другом порту:
```bash
php artisan serve --port=8001
```

Затем обновите `VITE_API_URL` в `frontend/.env`:
```env
VITE_API_URL=http://localhost:8001
```

### Проблема: "Class not found" или другие ошибки

**Решение:** Установите зависимости:
```bash
composer install
```

## 📝 После запуска backend:

1. **Запустите frontend** (в другом терминале):
   ```bash
   cd frontend
   npm run dev
   ```

2. **Откройте в браузере:** http://localhost:5173

3. **Нажмите "Войти через Yandex"**

## 🎯 Быстрый старт (оба сервера):

Используйте:
```bash
start.bat
```

Это запустит backend и frontend автоматически.

