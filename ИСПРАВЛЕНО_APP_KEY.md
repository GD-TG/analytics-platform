# ✅ Исправление: APP_KEY не установлен

## 🔴 Проблема:

```
EncryptionServiceProvider->key(...'key' => '')
```

Ошибка возникает потому, что `APP_KEY` не установлен в `.env` файле.

## ✅ Решение:

### Шаг 1: Проверьте наличие `.env` файла

Если файла `.env` нет, создайте его на основе `.env.example` (если есть) или создайте новый.

### Шаг 2: Сгенерируйте APP_KEY

Выполните в терминале:
```bash
php artisan key:generate
```

Эта команда:
- Создаст или обновит `.env` файл
- Добавит строку `APP_KEY=base64:...` с уникальным ключом

### Шаг 3: Очистите кеш

```bash
php artisan config:clear
php artisan cache:clear
```

## 📝 Альтернативный способ (если команда не работает):

Если `php artisan key:generate` не работает, вы можете:

1. **Создать `.env` файл** (если его нет) с базовыми настройками
2. **Вручную добавить APP_KEY:**

Откройте `.env` и добавьте:
```env
APP_KEY=base64:YOUR_KEY_HERE
```

Где `YOUR_KEY_HERE` - это случайная строка длиной 32 символа, закодированная в base64.

Или используйте онлайн генератор или выполните в PHP:
```php
<?php
echo 'base64:' . base64_encode(random_bytes(32));
```

## 🚀 После исправления:

1. Перезапустите backend сервер
2. Проверьте `/health`: http://localhost:8000/health
3. Попробуйте регистрацию снова

## ⚠️ Важно:

- `APP_KEY` должен быть уникальным для каждого приложения
- Никогда не коммитьте `.env` файл в git
- Если вы клонировали проект, всегда создавайте новый `.env` и генерируйте новый ключ

