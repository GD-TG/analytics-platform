# 🚀 Полная инструкция по настройке и запуску

## ✅ Все исправления применены:

1. ✅ Создан `config/cors.php` с правильными настройками
2. ✅ Улучшена обработка ошибок в HTTP клиенте
3. ✅ Улучшена обработка ошибок в контроллерах авторизации
4. ✅ Созданы batch файлы для автоматизации

## 📋 Пошаговая инструкция:

### Шаг 1: Проверка окружения

Запустите `check-setup.bat` для проверки всех зависимостей.

### Шаг 2: Настройка базы данных

1. **Создайте базу данных MySQL:**
   ```sql
   CREATE DATABASE analytics_platform CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

2. **Настройте `.env` файл:**
   ```env
   DB_CONNECTION=mysql
   DB_HOST=127.0.0.1
   DB_PORT=3306
   DB_DATABASE=analytics_platform
   DB_USERNAME=your_username
   DB_PASSWORD=your_password
   ```

3. **Выполните миграции:**
   - Запустите `migrate.bat`
   - Или вручную: `php artisan migrate`

### Шаг 3: Очистка кеша

Запустите `setup.bat` для очистки всех кешей Laravel.

### Шаг 4: Установка зависимостей (если нужно)

**Backend:**
```bash
composer install
```

**Frontend:**
```bash
cd frontend
npm install
```

### Шаг 5: Запуск серверов

**Вариант 1: Автоматический запуск обоих серверов**
```bash
start.bat
```

**Вариант 2: Запуск отдельно**
- Backend: `start-backend.bat`
- Frontend: `start-frontend.bat`

### Шаг 6: Проверка работы

1. **Backend:** http://localhost:8000/health
   - Должен вернуть: `{"status":"ok","timestamp":"..."}`

2. **Frontend:** http://localhost:5173
   - Должна открыться страница входа

3. **Создайте первого пользователя:**
   ```bash
   php artisan tinker
   ```
   ```php
   \App\Models\User::create([
       'name' => 'Admin',
       'email' => 'admin@example.com',
       'password' => bcrypt('password'),
       'role' => 'admin',
   ]);
   ```

## 🔧 Решение проблем с "Failed to fetch"

### Если ошибка все еще есть:

1. **Убедитесь, что оба сервера запущены:**
   - Backend: http://localhost:8000
   - Frontend: http://localhost:5173

2. **Очистите кеш:**
   - Запустите `setup.bat`

3. **Проверьте CORS:**
   - Файл `config/cors.php` должен содержать `http://localhost:5173`

4. **Проверьте консоль браузера (F12):**
   - Откройте вкладку Console
   - Посмотрите точное сообщение об ошибке
   - Проверьте вкладку Network - какой запрос не проходит

5. **Проверьте базу данных:**
   - Убедитесь, что миграции выполнены
   - Проверьте подключение к БД

## 📁 Созданные файлы:

- `setup.bat` - очистка кеша и оптимизация
- `migrate.bat` - выполнение миграций
- `check-setup.bat` - проверка окружения
- `start.bat` - запуск обоих серверов
- `start-backend.bat` - запуск backend
- `start-frontend.bat` - запуск frontend
- `config/cors.php` - настройки CORS
- `FIX_CORS.md` - подробная инструкция

## 🎯 Быстрый старт:

1. Запустите `check-setup.bat` - проверка
2. Запустите `migrate.bat` - миграции БД
3. Запустите `setup.bat` - очистка кеша
4. Запустите `start.bat` - запуск серверов
5. Откройте http://localhost:5173

## ⚠️ Важно:

- Backend должен быть на порту **8000**
- Frontend должен быть на порту **5173**
- База данных должна быть настроена и миграции выполнены
- После изменения `config/cors.php` нужно очистить кеш (`setup.bat`)

